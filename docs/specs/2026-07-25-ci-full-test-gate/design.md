# Design

验证脚本保留编译、Git 空白检查和项目隔离检查，将默认 pytest 参数改为不传具体文件。pytest 从 `pyproject.toml` 的 `testpaths` 发现 `tests/` 下匹配规则的测试文件。

历史代码删除后遗留的测试按职责处理：只依赖已删除架构的文件直接删除；包含当前运行时行为的文件则只移除失效导入、fixture 和断言，保留其余当前接口测试。不得为让测试通过重新引入已删除模块或空壳兼容层。

修复过程分两轮：先解决 collection error，再运行完整 suite 并逐个修复真实失败。完成后使用验证脚本本身作为最终门禁。
